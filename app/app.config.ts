export default defineAppConfig({
  ui: {
    popover: {
      slots: {
        arrow: "fill-current",
        content: "py-2 px-3"
      }
    },
    dropdownMenu: {
      slots: {
        arrow: "fill-current"
      }
    }
  }
});
